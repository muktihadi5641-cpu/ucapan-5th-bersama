# (audio MP3 files are now served directly — no longer excluded)

# Vercel local link state
.vercel/

# IDE binary (huge, not part of the project)
*.exe

# Reference video (not used at runtime)
WhatsApp Video*.mp4

# Server logs / scratch
*.log
*.tmp

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/

# Raw HEIC originals (already converted to .jpg)
photos/berdua/*.HEIC
photos/berdua/2026(1).jpeg
photos/berdua/2026(2).jpeg

# Deploy / debug preview screenshots
v-*.png
v.png
preview/
